# Jekyll 博客本地运行指南（Windows CMD）

## 方法一：使用 Bundle（推荐）

### 步骤：

1. **打开 CMD 命令提示符**
   - 按 `Win + R`，输入 `cmd`，回车
   - 或者按 `Win + X`，选择"命令提示符"

2. **切换到项目目录**
   ```cmd
   cd /d E:\github\lyzbcy.github.io
   ```

3. **安装依赖（如果还没安装）**
   ```cmd
   bundle install
   ```
   ⚠️ 如果提示找不到 `bundle` 命令，需要先安装 Ruby 和 Bundler

4. **启动本地服务器**
   ```cmd
   bundle exec jekyll serve
   ```
   或者：
   ```cmd
   bundle exec jekyll s
   ```

5. **访问本地博客**
   - 在浏览器中打开：`http://localhost:4000`
   - 或者：`http://127.0.0.1:4000`

6. **停止服务器**
   - 在 CMD 中按 `Ctrl + C`

---

## 方法二：直接使用本地 Jekyll（如果已安装）

如果你看到 `vendor\bundle\ruby\3.4.0\bin\jekyll.bat` 文件存在，可以直接使用：

```cmd
cd /d E:\github\lyzbcy.github.io
vendor\bundle\ruby\3.4.0\bin\jekyll.bat serve
```

---

## 常见问题解决

### 1. 提示找不到 `bundle` 或 `jekyll` 命令

**解决方案：安装 Ruby 和 Bundler**

1. 下载 Ruby 安装包：
   - 访问：https://rubyinstaller.org/downloads/
   - 下载 Ruby+Devkit 版本（推荐 3.3.x 或 3.4.x）

2. 安装 Ruby：
   - 运行安装程序，勾选 "Add Ruby executables to your PATH"
   - 安装完成后，重新打开 CMD

3. 安装 Bundler：
   ```cmd
   gem install bundler
   ```

4. 然后回到项目目录运行：
   ```cmd
   bundle install
   bundle exec jekyll serve
   ```

### 2. 端口被占用（Port 4000 already in use）

**解决方案：使用其他端口**
```cmd
bundle exec jekyll serve --port 4001
```

### 3. 修改后页面没有更新

**解决方案：强制重新构建**
```cmd
bundle exec jekyll serve --force_polling
```

### 4. 查看实时日志和错误信息

启动后，Jekyll 会显示：
- 服务器地址
- 构建信息
- 错误和警告

---

## 快速命令参考

```cmd
# 进入项目目录
cd /d E:\github\lyzbcy.github.io

# 安装依赖
bundle install

# 启动服务器（默认端口 4000）
bundle exec jekyll serve

# 启动服务器（指定端口）
bundle exec jekyll serve --port 4001

# 启动服务器（强制重新构建）
bundle exec jekyll serve --force_polling

# 仅构建不启动服务器
bundle exec jekyll build

# 查看帮助
bundle exec jekyll help
```

---

## 提示

- ✅ 修改 `_posts` 目录下的文章后，页面会自动刷新
- ✅ 修改 `_config.yml` 后需要重启服务器（Ctrl+C 然后重新运行）
- ✅ 修改 CSS/JS 文件后可能需要强制刷新浏览器（Ctrl+F5）
- ✅ 本地预览时，网站 URL 是 `http://localhost:4000`，不是 `https://lyzbcy.github.io`

---

## 现在就开始！

打开 CMD，复制粘贴以下命令：

```cmd
cd /d E:\github\lyzbcy.github.io
bundle exec jekyll serve
```

然后在浏览器中打开 `http://localhost:4000` 就可以看到你的博客了！🎉

