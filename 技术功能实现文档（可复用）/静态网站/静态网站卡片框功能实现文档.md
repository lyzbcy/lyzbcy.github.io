# 静态网站卡片框功能实现文档

## 功能概述

卡片框是一个用于展示可折叠内容的交互式组件，支持：
- 📦 可折叠/展开的内容卡片
- 🎨 多种样式变体（标准、紧凑、已废止等）
- 📱 响应式设计，自动适配移动端和桌面端
- 🔄 流畅的动画效果和交互反馈
- 📋 支持单列或双列布局
- ✨ 精美的渐变背景和悬停效果

## 文件结构

```
项目根目录/
├── assets/
│   └── lib-custom/
│       └── order-card.css          # 卡片框样式文件
├── _includes/
│   └── head.html                   # 或 custom-head.html（引用CSS文件）
└── _posts/
    └── XXX.md                      # Markdown 文章文件（使用卡片框）
```

## HTML 结构

### 1. 基础结构

```html
<div class="orders-container">
  <details class="order-card">
    <summary>卡片标题</summary>
    <div class="content">
      <p>卡片内容可以包含任何HTML元素</p>
      <ul>
        <li>列表项</li>
      </ul>
      <a href="#">链接</a>
    </div>
  </details>
</div>
```

### 2. 完整示例

```html
<div class="orders-container">
  <details class="order-card">
    <summary>标准卡片</summary>
    <div class="content">
      <p>这是标准样式的卡片，标题有紫色渐变背景。</p>
      <p>内容区域是白色背景，支持任何HTML内容。</p>
    </div>
  </details>
  
  <details class="order-card order-card--compact">
    <summary>紧凑卡片</summary>
    <div class="content">
      <p>这是紧凑样式的卡片，标题没有背景色，右侧有下拉箭头。</p>
      <p>适合用于FAQ、问题解答等场景。</p>
    </div>
  </details>
  
  <details class="order-card not-done">
    <summary>未完成卡片</summary>
    <div class="content">
      <p>这是未完成状态的卡片，标题是灰色渐变，整体透明度降低。</p>
    </div>
  </details>
  
  <details class="order-card order-card--deprecated">
    <summary>
      已废止方案
      <span class="order-card__badge order-card__badge--deprecated">已废除</span>
    </summary>
    <div class="content">
      <p>这是已废止方案的卡片，可以配合徽章使用。</p>
    </div>
  </details>
</div>
```

### 3. 单列布局

```html
<div class="orders-container orders-container--column">
  <details class="order-card">
    <summary>第一个卡片</summary>
    <div class="content">
      <p>单列布局，每个卡片占满整行。</p>
    </div>
  </details>
  
  <details class="order-card">
    <summary>第二个卡片</summary>
    <div class="content">
      <p>适合需要强调顺序或重要性的场景。</p>
    </div>
  </details>
</div>
```

## CSS 样式详解

### 1. 容器样式

```css
/* 双列布局容器（默认） */
.orders-container {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  margin: 15px 0;
  align-items: stretch;
}

/* 单列布局容器 */
.orders-container--column {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin: 12px 0 24px;
}
```

**说明：**
- `orders-container`：双列布局，每行显示两个卡片（移动端自动变为单列）
- `orders-container--column`：单列布局，每个卡片占满整行
- `gap`：卡片之间的间距
- `align-items: stretch`：确保卡片高度一致

### 2. 标准卡片样式

```css
.order-card {
  flex: 1 1 calc(50% - 7.5px);
  min-width: 300px;
  max-width: calc(50% - 7.5px);
  display: flex;
  flex-direction: column;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  background: #fafafa;
  transition: all 0.3s ease;
  overflow: hidden;
}

.order-card:hover {
  border-color: #999;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.order-card summary {
  padding: 12px 16px;
  cursor: pointer;
  font-weight: 600;
  font-size: 1.1em;
  color: #fff;
  user-select: none;
  border-radius: 8px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  margin: 0;
  word-wrap: break-word;
  overflow-wrap: break-word;
  white-space: normal;
  display: block;
  width: 100%;
  box-sizing: border-box;
}

.order-card[open] summary {
  border-radius: 8px 8px 0 0;
}

.order-card summary:hover {
  background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
}

.order-card summary::-webkit-details-marker {
  display: none;
}

.order-card summary::before {
  content: "▶ ";
  display: inline-block;
  margin-right: 8px;
  transition: transform 0.3s;
}

.order-card[open] summary::before {
  transform: rotate(90deg);
}

.order-card .content {
  padding: 16px;
  border-top: 1px solid #e0e0e0;
  background: #fff;
  border-radius: 0 0 8px 8px;
  flex: 1;
}
```

**关键特性：**
- 标题使用紫色渐变背景（`#667eea` → `#764ba2`）
- 悬停时渐变方向反转，提供视觉反馈
- 使用 `▶` 符号作为展开/折叠指示器，展开时旋转90度
- 内容区域白色背景，与标题形成对比

### 3. 紧凑卡片样式

```css
.order-card--compact {
  border-radius: 10px;
  background: linear-gradient(135deg, #f8f9ff 0%, #fefefe 100%);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
}

.order-card--compact summary {
  position: relative;
  font-size: 16px;
  padding: 14px 18px;
  color: #3a4a69;
  background: none;
  list-style: none;
}

.order-card--compact summary::before {
  content: none;
}

.order-card--compact summary::after {
  content: "▼";
  position: absolute;
  right: 18px;
  transition: transform 0.2s ease;
  color: #8a95b4;
}

.order-card--compact[open] summary::after {
  transform: rotate(-180deg);
}

.order-card--compact .content {
  padding: 16px 20px 20px;
  border-top: 1px solid #e7e9f3;
}
```

**关键特性：**
- 标题没有背景色，使用深色文字（`#3a4a69`）
- 使用 `▼` 符号作为指示器，位于右侧
- 整体背景使用淡蓝色渐变，更加柔和
- 适合用于FAQ、问题解答等场景

### 4. 未完成状态样式

```css
.order-card.not-done {
  background: #f5f5f5;
  border-color: #d0d0d0;
  opacity: 0.85;
}

.order-card.not-done summary {
  background: linear-gradient(135deg, #95a5a6 0%, #7f8c8d 100%);
  border-radius: 8px;
}

.order-card.not-done[open] summary {
  border-radius: 8px 8px 0 0;
}

.order-card.not-done summary:hover {
  background: linear-gradient(135deg, #7f8c8d 0%, #95a5a6 100%);
}
```

**关键特性：**
- 整体透明度降低（`opacity: 0.85`）
- 标题使用灰色渐变背景
- 用于表示未完成、待处理的状态

### 5. 已废止方案样式

```css
.order-card--deprecated {
  flex: 1 1 100%;
  max-width: 100%;
  border: 1px solid #d9d9d9;
  background: linear-gradient(135deg, #f7f7f7 0%, #fdfdfd 100%);
  box-shadow: inset 0 0 0 1px rgba(0, 0, 0, 0.02);
}

.order-card--deprecated summary {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  font-size: 16px;
  font-weight: 600;
  padding: 16px 20px;
  color: #4c4c4c;
  background: none;
  letter-spacing: 0.02em;
}

.order-card--deprecated summary::before {
  content: none;
}

.order-card--deprecated summary::after {
  content: "▼";
  font-size: 13px;
  color: #8c8c8c;
  transition: transform 0.2s ease;
}

.order-card--deprecated[open] summary::after {
  transform: rotate(-180deg);
}

.order-card--deprecated .content {
  padding: 18px 22px 22px;
  border-top: 1px dashed #d9d9d9;
  background: #fcfcfc;
}

.order-card__badge {
  font-size: 13px;
  padding: 2px 10px;
  border-radius: 999px;
  border: 1px solid #cfcfcf;
  background: #f3f3f3;
  color: #6b6b6b;
  white-space: nowrap;
}

.order-card__badge--deprecated {
  border-color: #c8b48f;
  color: #8a6a33;
  background: #f7f1e5;
}
```

**关键特性：**
- 占满整行宽度（`flex: 1 1 100%`）
- 标题使用深灰色文字，无背景
- 支持在标题中添加徽章（`order-card__badge`）
- 内容区域使用虚线边框分隔
- 用于表示已废止、不再使用的方案

### 6. 响应式设计

```css
@media (max-width: 768px) {
  .order-card {
    flex: 1 1 100%;
    max-width: 100%;
  }
}
```

**说明：**
- 在移动端（屏幕宽度 ≤ 768px）时，卡片自动变为单列布局
- 每个卡片占满整行宽度

## 使用方法

### 1. 引入CSS文件

在 `_includes/head.html` 或 `_includes/custom-head.html` 中添加：

```html
<link rel="stylesheet" href="{{ '/assets/lib-custom/order-card.css' | relative_url }}">
```

### 2. 在Markdown文章中使用

#### 基础用法（双列布局）

```markdown
<div class="orders-container">
  <details class="order-card">
    <summary>第一个卡片</summary>
    <div class="content">
      <p>这是第一个卡片的内容。</p>
    </div>
  </details>
  
  <details class="order-card">
    <summary>第二个卡片</summary>
    <div class="content">
      <p>这是第二个卡片的内容。</p>
    </div>
  </details>
</div>
```

#### 紧凑样式

```markdown
<div class="orders-container">
  <details class="order-card order-card--compact">
    <summary>问题1：如何使用？</summary>
    <div class="content">
      <p>答案内容...</p>
    </div>
  </details>
  
  <details class="order-card order-card--compact">
    <summary>问题2：如何配置？</summary>
    <div class="content">
      <p>答案内容...</p>
    </div>
  </details>
</div>
```

#### 单列布局

```markdown
<div class="orders-container orders-container--column">
  <details class="order-card">
    <summary>步骤1：准备工作</summary>
    <div class="content">
      <p>第一步的内容...</p>
    </div>
  </details>
  
  <details class="order-card">
    <summary>步骤2：开始实施</summary>
    <div class="content">
      <p>第二步的内容...</p>
    </div>
  </details>
</div>
```

#### 未完成状态

```markdown
<div class="orders-container">
  <details class="order-card not-done">
    <summary>待完成的任务</summary>
    <div class="content">
      <p>这个任务还没有完成。</p>
    </div>
  </details>
</div>
```

#### 已废止方案

```markdown
<div class="orders-container">
  <details class="order-card order-card--deprecated">
    <summary>
      旧方案
      <span class="order-card__badge order-card__badge--deprecated">已废除</span>
    </summary>
    <div class="content">
      <p>这是已经不再使用的旧方案。</p>
    </div>
  </details>
</div>
```

### 3. 内容支持

卡片内容区域支持所有HTML元素：

```html
<div class="orders-container">
  <details class="order-card">
    <summary>包含多种内容的卡片</summary>
    <div class="content">
      <p>段落文本</p>
      <ul>
        <li>列表项1</li>
        <li>列表项2</li>
      </ul>
      <blockquote>
        <p>引用内容</p>
      </blockquote>
      <p><a href="#" target="_blank">链接</a></p>
      <p><code>代码片段</code></p>
      <p><strong>粗体</strong> 和 <em>斜体</em></p>
    </div>
  </details>
</div>
```

## 样式变体总结

| 类名 | 说明 | 适用场景 |
|------|------|----------|
| `order-card` | 标准卡片 | 通用场景，标题有紫色渐变 |
| `order-card--compact` | 紧凑卡片 | FAQ、问题解答，标题无背景 |
| `order-card--deprecated` | 已废止卡片 | 旧方案、不再使用的内容 |
| `not-done` | 未完成状态 | 待处理、未完成的任务 |
| `orders-container` | 双列布局 | 默认布局，一行两个 |
| `orders-container--column` | 单列布局 | 强调顺序、重要性的场景 |

## 组合使用示例

### 示例1：FAQ页面

```html
<div class="orders-container">
  <details class="order-card order-card--compact">
    <summary>如何开始使用？</summary>
    <div class="content">
      <p>首先需要安装依赖，然后运行启动命令。</p>
    </div>
  </details>
  
  <details class="order-card order-card--compact">
    <summary>遇到问题怎么办？</summary>
    <div class="content">
      <p>可以查看文档或提交Issue。</p>
    </div>
  </details>
</div>
```

### 示例2：任务列表

```html
<div class="orders-container orders-container--column">
  <details class="order-card">
    <summary>已完成：功能开发</summary>
    <div class="content">
      <p>已完成的功能列表...</p>
    </div>
  </details>
  
  <details class="order-card not-done">
    <summary>待完成：测试和优化</summary>
    <div class="content">
      <p>待完成的任务列表...</p>
    </div>
  </details>
</div>
```

### 示例3：方案对比

```html
<div class="orders-container">
  <details class="order-card">
    <summary>新方案：推荐使用</summary>
    <div class="content">
      <p>新方案的详细说明...</p>
    </div>
  </details>
  
  <details class="order-card order-card--deprecated">
    <summary>
      旧方案
      <span class="order-card__badge order-card__badge--deprecated">已废除</span>
    </summary>
    <div class="content">
      <p>旧方案的说明，仅供参考...</p>
    </div>
  </details>
</div>
```

## 自定义样式

### 修改颜色主题

如果需要修改标准卡片的渐变颜色，可以覆盖CSS变量或直接修改样式：

```css
/* 自定义渐变颜色 */
.order-card summary {
  background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}

.order-card summary:hover {
  background: linear-gradient(135deg, #your-color-2 0%, #your-color-1 100%);
}
```

### 修改间距

```css
/* 修改卡片间距 */
.orders-container {
  gap: 20px; /* 默认15px */
}

/* 修改内容内边距 */
.order-card .content {
  padding: 20px; /* 默认16px */
}
```

### 修改圆角

```css
/* 修改卡片圆角 */
.order-card {
  border-radius: 12px; /* 默认8px */
}
```

## 浏览器兼容性

- ✅ Chrome/Edge（最新版本）
- ✅ Firefox（最新版本）
- ✅ Safari（最新版本）
- ✅ 移动端浏览器（iOS Safari、Chrome Mobile）

**注意事项：**
- 使用 `<details>` 标签，需要现代浏览器支持
- CSS Grid 和 Flexbox 需要现代浏览器支持
- 渐变背景和过渡动画需要现代浏览器支持

## 最佳实践

### 1. 标题长度

- 保持标题简洁明了，建议不超过50个字符
- 标题会自动换行，但过长的标题会影响视觉效果

### 2. 内容组织

- 每个卡片包含一个主题的内容
- 避免在单个卡片中放置过多内容
- 使用列表、引用等元素增强可读性

### 3. 布局选择

- **双列布局**：适合内容相似、重要性相当的卡片
- **单列布局**：适合有顺序关系、需要强调重要性的内容

### 4. 样式选择

- **标准卡片**：通用场景，视觉突出
- **紧凑卡片**：FAQ、问题解答，视觉柔和
- **已废止卡片**：旧方案、历史记录，视觉弱化

### 5. 响应式考虑

- 移动端会自动变为单列，无需额外处理
- 确保内容在移动端也能良好显示

## 故障排查

### 问题：卡片样式不显示

1. 检查CSS文件是否正确引入
2. 检查文件路径是否正确
3. 检查浏览器控制台是否有CSS加载错误

### 问题：卡片布局异常

1. 检查容器类名是否正确（`orders-container`）
2. 检查是否有其他CSS样式冲突
3. 检查浏览器是否支持Flexbox

### 问题：折叠/展开不工作

1. 检查是否使用了 `<details>` 标签
2. 检查是否有JavaScript阻止了默认行为
3. 检查浏览器是否支持 `<details>` 标签

## 扩展建议

### 1. 添加动画效果

可以添加更丰富的动画效果：

```css
.order-card .content {
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 2. 添加图标支持

可以在标题中添加图标：

```html
<summary>
  <span class="icon">📦</span>
  卡片标题
</summary>
```

### 3. 添加计数功能

可以显示卡片数量：

```html
<div class="orders-container">
  <h3>共 <span id="cardCount">0</span> 个卡片</h3>
  <!-- 卡片内容 -->
</div>
```

### 4. 添加搜索功能

可以添加JavaScript实现卡片搜索：

```javascript
function searchCards(keyword) {
  const cards = document.querySelectorAll('.order-card');
  cards.forEach(card => {
    const text = card.textContent.toLowerCase();
    card.style.display = text.includes(keyword.toLowerCase()) ? 'flex' : 'none';
  });
}
```

## 总结

卡片框功能实现了一个完整的、可复用的内容展示组件，主要优势包括：

- ✅ **易用性**：简单的HTML结构，无需JavaScript
- ✅ **灵活性**：多种样式变体，适应不同场景
- ✅ **响应式**：自动适配移动端和桌面端
- ✅ **美观性**：精美的渐变背景和动画效果
- ✅ **可扩展**：易于自定义样式和添加新功能

通过这个文档，开发者可以理解整个组件的结构和实现细节，并在其他项目中复用或改进这个功能。

