---
title: "💪 健身数据追踪 — 捞鱼的训练日志"
date: 2026-07-25 14:21:00 +0800
categories: [健身, 数据]
tags: [健身, 训练, PPL, 渐进超负荷]
pin: true
toc: true
comments: true
---

> ⏱️ 自动化训练追踪 | 数据来源：每次训练后即刻记录

---

## 📊 训练概览

| 指标 | 数值 |
|------|------|
| 总训练次数 | **{{ site.data.fitness.overview.total_sessions }}** 次 |
| 周均频率 | **{{ site.data.fitness.overview.avg_sessions_per_week }}** 次/周 |
| 近4周训练 | **{{ site.data.fitness.overview.total_sessions_recent_4w }}** 次 |
| 总训练组数 | **{{ site.data.fitness.overview.total_sets }}** 组 |
| 总容量 | **{{ site.data.fitness.overview.total_volume | divided_by: 1000.0 | round: 1 }}k kg** |
| 记录起始 | {{ site.data.fitness.overview.date_range.first }} |
| 最后更新 | {{ site.data.fitness.generated_at | date: "%Y-%m-%d %H:%M" }} |

{% if site.data.fitness.overview.preferences.currentStatus %}
> ⚠️ **当前状态**：{{ site.data.fitness.overview.preferences.currentStatus }} — {{ site.data.fitness.overview.preferences.adjustmentNote }}
{% endif %}

---

## 🏆 个人记录 Top 15

{% assign sorted_prs = site.data.fitness.personal_records | sort: "volume" | reverse %}

| # | 动作 | 记录 | 总量 |
|---|------|------|------|
{% for pr in sorted_prs limit: 15 -%}
| {{ forloop.index }} | {{ pr.exercise }} | {{ pr.weight }}kg × {{ pr.reps }}{% if pr.note != "" %} · {{ pr.note }}{% endif %} | **{{ pr.volume }}kg** |
{% endfor %}

---

## ⚖️ 肌肉训练量分布

{% assign max_muscle = site.data.fitness.muscle_balance[0].total_volume | plus: 0 %}

| 肌群 | 总训练量 |
|------|----------|
{% for m in site.data.fitness.muscle_balance -%}
| {{ m.label }} | **{{ m.total_volume | divided_by: 1000.0 | round: 1 }}k kg** {% assign pct = m.total_volume | times: 100.0 | divided_by: max_muscle | round: 0 %} <progress value="{{ pct }}" max="100" style="width:100px;height:12px;"></progress> {{ pct }}% |
{% endfor %}

---

## 📈 周训练量趋势（近12周）

{% assign trend_count = site.data.fitness.volume_trend | size %}{% assign start_idx = trend_count | minus: 12 %}{% if start_idx < 0 %}{% assign start_idx = 0 %}{% endif %}

| 周 | 容量 | 组数 |
|----|------|------|
{% for w in site.data.fitness.volume_trend offset: start_idx -%}
| {{ w.week }} | **{{ w.volume | divided_by: 1000.0 | round: 1 }}k kg** | {{ w.sessions }} |
{% endfor %}

---

## 📅 训练记录

| 日期 | 类型 | 动作数 | 组数 | 容量 | 时长 | 备注 |
|------|------|--------|------|------|------|------|
{% for s in site.data.fitness.sessions -%}
| {{ s.date }} | `{{ s.split_label }}` | {{ s.exercises | size }} | {{ s.total_sets }} | **{{ s.total_volume | divided_by: 1000.0 | round: 1 }}k** | {% if s.duration_min %}{{ s.duration_min }}'{% else %}-{% endif %} | {{ s.note }} |
{% endfor %}

---

## 🏋️ 动作索引

| 动作 | 总组数 | 最大重量 | 最高次数 | 总容量 | 最近训练 |
|------|--------|----------|----------|--------|----------|
{% for ex in site.data.fitness.exercises -%}
| **{{ ex.name }}** | {{ ex.total_sets }} | {{ ex.max_weight }}kg | {{ ex.max_reps }}次 | {{ ex.total_volume | divided_by: 1000.0 | round: 1 }}k | {{ ex.last_date }} |
{% endfor %}

---
*📝 数据自动生成，每次训练后更新 · [查看数据脚本]({{ site.url }}/tools/generate_fitness_data.py)*
